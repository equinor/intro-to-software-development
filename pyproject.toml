[build-system]
requires = ["setuptools>=64.0", "setuptools_scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "geo-calculator"
authors = [{ name = "Mattias A. Pedersen-Estrada" }]
description = "Introduction to Software Development course and demo Python package"
readme = "README.md"
requires-python = ">=3.13"
license = { file = "LICENSE" }
dependencies = ["numpy"]
dynamic = ["version"]

[project.urls]
repository = "https://github.com/mattiasestrada/intro-to-software-development"

[project.optional-dependencies]
dev = ["black", "mypy", "pytest"]

[tool.mypy]
exclude = ["build"]
files = ["src", "tests"]